---
dependencies:
  - role: geerlingguy.pip
    pip_install_packages:
      - requests
      - poster
galaxy_info:
  role_name: ansible_role_notary
  author: mongodb
  description: Installs the notary client
  license: Apache
  min_ansible_version: 2.8
  platforms:
    - name: debian
      versions:
        - buster
  galaxy_tags:
    - notary
